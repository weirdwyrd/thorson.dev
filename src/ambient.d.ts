type Quote = {
    author: string,     // who wrote/spoke it
    material: string,   // title of what they wrote/spoke it in
    text: string        // what they said
}

type Coordinates = [number, number, number];